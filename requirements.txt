# HTTP client — used by url_discovery, jina_reader, wikipedia_fallback, uk/companies_house
httpx>=0.28.1

# HTML parsing — used by url_discovery (BFS link extraction) and parser
beautifulsoup4>=4.12.3
lxml>=5.3.0

# Data models & validation
pydantic>=2.10.0

# Domain extraction — used by url_discovery and validator
tldextract>=5.1.3

# Regex extensions — used by extractor and validator
regex>=2024.11.6

# Terminal output formatting — used by telemetry
rich>=13.9.4

# Environment variable loading
python-dotenv>=1.0.1

# Web framework & server
fastapi>=0.115.0
uvicorn>=0.32.0
jinja2>=3.1.4
python-multipart>=0.0.12
